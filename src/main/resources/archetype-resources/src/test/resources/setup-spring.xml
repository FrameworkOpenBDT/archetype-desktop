<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<context:component-scan
		base-package="${package}* io.openbdt.run* io.openbdt.setup*" />

	<bean class="io.openbdt.driver.DesktopDriver" id="desktopDriver"
		scope="singleton"/>

	<bean class="io.openbdt.run.SuiteTestDesktopRunner" id="suiteTestDesktopRunner"
		depends-on="desktopDriver">
		<property name="desktopDriver" ref="desktopDriver" />
	</bean>

	<bean class="${package}.pages.PageObjectClass" id="pageObject"
		init-method="initElements" depends-on="desktopDriver">
		<property name="driverLoader" ref="desktopDriver" />
	</bean>
	<bean class="io.openbdt.element.WindowScreenElement" id="viewElement"
		depends-on="pageObject" destroy-method="">
		<constructor-arg name="applicationView"
			value="#{pageObject.getApplicationView()}" />
	</bean>
</beans>